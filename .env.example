# URL de l'API sociale à interroger depuis le frontend
# - http://localhost:3030 pour utiliser `npm run mock:api` (valeur par défaut)
# - http://localhost:3031 pour utiliser `npm run live:api`
# - http://localhost:4000 (ou autre) pour votre backend maison
VITE_SOCIAL_API_URL=http://localhost:3030

# Laisser "true" pour utiliser automatiquement les données de démonstration
# lorsque votre backend n'est pas accessible.
# Passer à "false" uniquement si vous voulez tester exclusivement votre backend.
VITE_ALLOW_MOCK_FALLBACK=true

# Variables nécessaires au proxy live (`npm run live:api`).
# Laissez-les vides si vous n'utilisez pas ce proxy.
INSTAGRAM_SESSION_ID=
FACEBOOK_ACCESS_TOKEN=
YOUTUBE_API_KEY=
TWITTER_BEARER_TOKEN=

# Optionnel : cible du proxy pour `npm run start` afin de relayer les requêtes `/api/social`.
# Exemple : SOCIAL_PROXY_TARGET=https://mon-backend.example
SOCIAL_PROXY_TARGET=
